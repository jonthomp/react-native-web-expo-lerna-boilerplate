{
  "name": "react-native-web-expo-lerna-boilerplate",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "lerna-publish": "npx lerna publish --skip-npm",
    "lerna-bootstrap": "npx lerna bootstrap"
  },
  "commitlint": {
    "extends": [
      "@commitlint/config-conventional"
    ]
  },
  "devDependencies": {
    "@commitlint/cli": "^7.5.2",
    "@commitlint/config-conventional": "^7.5.0",
    "husky": "^1.3.1",
    "lerna": "^3.13.1",
    "prettier": "^1.16.4"
  }
}
